# secrets & local state
.env
.env.*

# local build output
bin/
dist/

# private ops docs & personal config (public repo ships README + roscoe.example.json)
ARCHITECTURE.md
roscoe.json

# working memory for `roscoe loop`: rewritten every iteration, so it is local
# state rather than something to version. The durable record is the ledger.
loop.md

.DS_Store
